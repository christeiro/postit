<% if obj.errors.any? %>
  <div class="row">
    <div class="alert alert-error span4">
      <h5>There were some errors:</h5>
      <ul>
        <% obj.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>